<div class="comment-form">
  <%= form_with(model: [blog, comment]) do |form| %>
    <div class="field">
      <%= form.label :description, "コメント内容" %>
      <%= form.text_area :description %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
